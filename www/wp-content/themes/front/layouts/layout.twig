<!DOCTYPE html>
<html lang="{{ langs.current }}">
	{% include 'core/head' with head|merge({
		allow: ['app-css', 'app-js'],
		dequeue: [
			'wp-block-library-css',
			'safe-svg-svg-icon-style-inline-css',
			'classic-theme-styles-inline-css',
			'global-styles-inline-css'
		]
	}) %}

	<body>
		{% include 'onces/admin-widget' only %}
		{% include 'onces/loader' only %}
		{% include 'onces/header' with menu only %}

		{% include 'atoms/scrollbar' with {
			is_main: true
		} only %}

		<div class="layout" v-barba="wrapper">
			{% block content %}
				{# Page content #}
			{% endblock %}
		</div>

		<div class="frame"></div>
	</body>

	{% include 'core/foot' with foot|merge({
		api: api,
		allow: [],
		dequeue: []
	}) %}
</html>
