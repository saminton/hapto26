{% extends 'layouts/layout.twig' %}
{% block content %}
	<main class="privacy"
		v-barba="container"
		v-barba-namespace="privacy"
		v-scroll="main"
		data-ancestor="{{ ancestor_id }}"
		data-id="{{ id }}">
		<div v-flux="default">
			<h1 v-font="title_1">
				{{ post.title }}
			</h1>
			<div v-margin="default_top" v-font="format">
				{{ post.content|autop }}
			</div>
		</div>
	</main>
{% endblock %}
